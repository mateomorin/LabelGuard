import logging

from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
import numpy as np
import pandas as pd


def extract_X_y(
        df_real: pd.DataFrame,
        df_synth: pd.DataFrame
        ) -> tuple[np.ndarray]:
    
    # TODO: create embeddings and labels

    pass
